# Redirects for Mon Toit Platform (Netlify/Vercel)

# SPA Fallback - All routes should serve index.html
/*    /index.html   200

# API Routes (if using serverless functions)
/api/*  /.netlify/functions/:splat  200

# Force HTTPS
http://* https://:splat 301!

# WWW to non-WWW redirect (adjust based on your domain preference)
# https://www.montoit.ci/* https://montoit.ci/:splat 301!
